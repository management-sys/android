package com.example.attendancemanagementapp.ui.project.personnel

object ProjectPersonnelReducer {
    fun reduce(s: ProjectPersonnelState, e: ProjectPersonnelEvent): ProjectPersonnelState = when (e) {
        else -> s
    }
}