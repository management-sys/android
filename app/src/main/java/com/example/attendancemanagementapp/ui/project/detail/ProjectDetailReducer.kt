package com.example.attendancemanagementapp.ui.project.detail

object ProjectDetailReducer {
    fun reduce(s: ProjectDetailState, e: ProjectDetailEvent): ProjectDetailState = when (e) {
        else -> s
    }
}